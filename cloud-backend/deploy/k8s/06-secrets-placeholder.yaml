# 凭证入口（文档：K8s Secret + External Secrets，代码仓库零明文凭证）。
# 生产环境请替换为 ExternalSecret 指向 Vault，而不是直接写明文。
apiVersion: v1
kind: Secret
metadata:
  name: model-credentials
  namespace: cloude-backend
type: Opaque
stringData:
  # 仅示例占位；真实值由 External Secrets Operator 同步
  admin-token: "CHANGE_ME"
